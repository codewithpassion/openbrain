{
  "name": "@openbrains/scripts",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "smoke": "bun run smoke.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@openbrains/cli": "workspace:*",
    "@openbrains/shared": "workspace:*",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
